name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Cancel superseded runs on the same branch to save minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.12', '3.14']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install test dependencies
        run: pip install -r config/requirements-dev.txt

      - name: Run test suite
        env:
          # Tests exercise pure logic only and never open a real connection —
          # database.service just needs DATABASE_URL present at import time.
          DATABASE_URL: postgresql://test:test@localhost:5432/test
        run: PYTHONPATH=src pytest src/backend/tests/ -v --tb=short
