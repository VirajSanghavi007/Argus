# Virtual environment
venv/
.venv/
env/

# Python bytecode
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Pytest / test cache
.pytest_cache/
.cache/
htmlcov/
.coverage
*.cover

# Dataset files (too large for git)
data/

# Validation output (regenerated on each run)
data/validation_results.json

# whitelist.json is intentionally committed â€” do NOT add it here
# data/whitelist.json

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
*.env
kaggle.json

# Build / dist
dist/
build/
*.egg-info/

# Logs
logs/
*.log

# Model weights
*.pt

# Node modules
node_modules/

# pipeline_cache.json and fraud_model.pkl are intentionally committed
# pipeline_cache.json slimmed to ~20MB (transactions capped at 150/alert)
# fraud_model.pkl is ~648KB
data/*.csv

# Local pip wheel cache — never commit (huge, platform-specific)
wheels/
