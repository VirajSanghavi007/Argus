# Core API + demo serving (dashboard, investigate, case manager, whitelist).
# These run off the committed pipeline_cache.json — NO torch needed at boot.
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.4
slowapi
python-multipart
pandas==3.0.2
numpy==2.4.4
networkx==3.6.1
scikit-learn==1.8.0
psycopg2-binary

# ── Optional ML deps (Predict tab + live rescore) ────────────────────────────
# Heavy (~800MB). Uncomment + add to buildCommand only if the Render plan can
# handle torch. The core demo works without these.
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch==2.12.1
# torch_geometric==2.8.0
